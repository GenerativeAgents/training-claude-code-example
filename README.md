# training-claude-code-example

## 使い方

### 1. プロジェクトの作成

```bash
git clone [このリポジトリ] training-claude-code-example
cd training-claude-code-example
```

### 2. Devcontainerで開く

VS Codeで「Reopen in Container（コンテナで再度開く）」を選択すると、自動的に:
- Node.js LTS環境が構築される
- npm installが実行される
